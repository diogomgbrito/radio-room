# K8s — deploy everything at once
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: radio-room
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
