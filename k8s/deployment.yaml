apiVersion: apps/v1
kind: Deployment
metadata:
  name: radio-room
  namespace: radio-room
  labels:
    app: radio-room
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radio-room
  template:
    metadata:
      labels:
        app: radio-room
    spec:
      containers:
        - name: radio-room
          image: radio-room:latest
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
          volumeMounts:
            - name: data
              mountPath: /app/data
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: radio-room-data
